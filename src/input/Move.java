package input;

import events.Event;
import events.Events;
import world.Entity;

public class Move {
	double deltaX, deltaY;
	Entity entity;
	
	public Move(){
		Event e = new 
		notify(Events.newEvent(Events.EventType.MOVE));
	}
}
